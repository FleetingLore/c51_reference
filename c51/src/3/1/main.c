#include <REGX52.H>

void main() {
    while (1) {
        // `P3_1` 控制 `K1`，`P3_0` 控制 `K2`
        if (P3_1 == 0 || P3_0 == 0) {
            /// 如果按下 `K1` 或 `K2`，灯就亮
            P2_0 = 0;
        } else {
            /// 松开，灯就灭
            P2_0 = 1;
        }
    }
}